# Используйте официальный образ Nginx
FROM nginx:latest

# Копируйте ваш HTML файл в директорию, где Nginx будет искать файлы
COPY ./ /usr/share/nginx/html

# Опционально, укажите порт, на котором будет работать веб-сервер
EXPOSE 80:80
